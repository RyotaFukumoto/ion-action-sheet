import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ion-action-sheet',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
